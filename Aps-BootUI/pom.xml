<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <artifactId>ruoyi-bootui</artifactId>
        <groupId>com.ruoyi</groupId>
        <version>4.7.6-SNAPSHOT</version>
        <relativePath />
    </parent>

    <groupId>com.zlt</groupId>
    <artifactId>aps-bootui</artifactId>
    <version>1.0.3-SNAPSHOT</version>
    <name>aps-bootui</name>
    <description>Demo project for Spring Boot</description>

    <properties>
        <ruoyi.version>4.7.6-SNAPSHOT</ruoyi.version>
        <ruoyi-cloud.version>3.5.2-SNAPSHOT</ruoyi-cloud.version>
        <redisson.version>3.9.1</redisson.version>
        <quartz.version>2.3.2</quartz.version>
        <java.version>1.8</java.version>
        <aps-api.version>1.0.3-SNAPSHOT</aps-api.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.zlt</groupId>
            <artifactId>zlt-frame-starter</artifactId>
            <version>${ruoyi.version}</version>
        </dependency>

        <dependency>
            <groupId>com.zlt</groupId>
            <artifactId>zlt-ui-freeItem</artifactId>
            <version>${ruoyi.version}</version>
        </dependency>
        <dependency>
            <groupId>com.zlt</groupId>
            <artifactId>zlt-ui-billcode</artifactId>
            <version>${ruoyi.version}</version>
        </dependency>
        <dependency>
            <groupId>com.zlt</groupId>
            <artifactId>zlt-ui-sys-def</artifactId>
            <version>${ruoyi.version}</version>
        </dependency>
        <dependency>
            <groupId>com.ruoyi</groupId>
            <artifactId>ruoyi-api-gateway-system</artifactId>
        </dependency>
        <dependency>
            <groupId>com.zlt</groupId>
            <artifactId>zlt-ui-dataauth</artifactId>
            <version>${ruoyi.version}</version>
        </dependency>
       <!-- <dependency>
            <groupId>com.zlt</groupId>
            <artifactId>zlt-kettle-log-api</artifactId>
            <version>${ruoyi-cloud.version}</version>
        </dependency>-->
<!--        <dependency>
            <groupId>com.zlt</groupId>
            <artifactId>zlt-ui-kettle</artifactId>
            <version>${ruoyi.version}</version>
        </dependency>-->
        <dependency>
            <groupId>com.zlt</groupId>
            <artifactId>zlt-ui-message</artifactId>
            <version>${ruoyi.version}</version>
        </dependency>
       <!-- <dependency>
            <groupId>com.zlt</groupId>
            <artifactId>zlt-cas-client</artifactId>
            <version>${ruoyi.version}</version>
        </dependency>-->
<!--        <dependency>
            <groupId>com.zlt</groupId>
            <artifactId>zlt-frame-core</artifactId>
            <version>${ruoyi.version}</version>
        </dependency>-->

        <dependency>
            <groupId>org.redisson</groupId>
            <artifactId>redisson</artifactId>
            <version>${redisson.version}</version>
        </dependency>

        <!-- Quartz -->
      <!--  <dependency>
            <groupId>org.quartz-scheduler</groupId>
            <artifactId>quartz</artifactId>
            <version>${quartz.version}</version>
        </dependency>-->

        <dependency>
            <groupId>com.github.xiaoymin</groupId>
            <artifactId>knife4j-spring-boot-starter</artifactId>
            <version>2.0.2</version>
        </dependency>

        <dependency>
            <groupId>com.zlt.aps</groupId>
            <artifactId>aps-all-api</artifactId>
            <version>${aps-api.version}</version>
        </dependency>
        <dependency>
            <groupId>com.ruoyi.file</groupId>
            <artifactId>aps-file-api</artifactId>
            <version>${aps-api.version}</version>
        </dependency>
        <dependency>
            <groupId>com.zlt.aps</groupId>
            <artifactId>aps-mp-api</artifactId>
            <version>${aps-api.version}</version>
        </dependency>
        <dependency>
            <groupId>com.zlt</groupId>
            <artifactId>Mix-Bootui</artifactId>
            <version>${aps-api.version}</version>
        </dependency>

        <dependency>
            <groupId>com.zlt.aps</groupId>
            <artifactId>aps-cx-lh-api</artifactId>
            <version>${aps-api.version}</version>
        </dependency>
        <!-- 导入文件解密 -->
      <!--  <dependency>
            <groupId>com.zlt</groupId>
            <artifactId>zlt-ll-file-encrypt</artifactId>
            <version>3.1.4-SNAPSHOT</version>
        </dependency>-->

        <dependency>
            <groupId>cn.hutool</groupId>
            <artifactId>hutool-all</artifactId>
            <version>5.8.25</version>
        </dependency>
    </dependencies>

    <distributionManagement>
        <snapshotRepository>
            <id>local-snapshot</id>
            <url>http://192.168.2.95:8081/nexus/content/repositories/snapshots/</url>
        </snapshotRepository>

        <repository>
            <id>local-release</id>
            <url>http://192.168.2.95:8081/nexus/content/repositories/releases/</url>
        </repository>
    </distributionManagement>

    <repositories>
        <repository>
            <id>local-snapshot</id>
            <url>http://192.168.2.95:8081/nexus/content/groups/public</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <!-- 默认不下载快照版本，这里开启下载快照版本到本地仓库 -->
                <enabled>true</enabled>
            </snapshots>
        </repository>
    </repositories>
    <build>
        <plugins>
            <plugin>
                <!--
                    1. 加密后,方法体被清空,保留方法参数、注解等信息.主要兼容swagger文档注解扫描
                    2. 方法体被清空后,反编译只能看到方法名和注解,看不到方法体的具体内容
                    3. 加密后的项目需要设置javaagent来启动,启动过程中解密class,完全内存解密,不留下任何解密后的文件
                    4. 启动加密后的jar,生成xxx-encrypted.jar,这个就是加密后的jar文件,加密后不可直接执行
                    5. 无密码启动方式,java -javaagent:xxx-encrypted.jar -jar xxx-encrypted.jar
                    6. 有密码启动方式,java -javaagent:xxx-encrypted.jar='-pwd= 密码' -jar xxx-encrypted.jar
                -->
                <groupId>net.roseboy</groupId>
                <artifactId>classfinal-maven-plugin</artifactId>
                <version>1.2.1</version>
                <configuration>
                    <password>#</password><!-- #表示启动时不需要密码,事实上对于代码混淆来说,这个密码没什么用,它只是一个启动密码 -->
                    <packages>com.zlt</packages><!-- 加密的包名,多个包用逗号分开 -->
                </configuration>
                <executions>
                    <execution>
                        <phase>install</phase>
                        <goals>
                            <goal>classFinal</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring-boot.version}</version>
                <configuration>
                    <fork>true</fork> <!-- 如果没有该配置，devtools不会生效 -->
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
        <finalName>${project.artifactId}</finalName>

    </build>

</project>
