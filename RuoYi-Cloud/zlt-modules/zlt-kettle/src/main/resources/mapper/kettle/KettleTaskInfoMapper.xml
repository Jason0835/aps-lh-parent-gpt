<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zlt.kettle.mapper.KettleTaskInfoMapper">

    <resultMap type="TaskInfo" id="TaskInfoResult">
        <id property="id" column="id"/>
        <result property="categoryId" column="category_id"/>
        <result property="taskName" column="task_name"/>
        <result property="taskType" column="task_type"/>
        <result property="remoteUrl" column="remote_url"/>
        <result property="lastRuntime" column="last_runtime"/>
        <result property="recordCount" column="record_count"/>

        <result property="delFlag" column="del_flag"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="getTaskInfoList" parameterType="TaskInfo" resultMap="TaskInfoResult">
        select t.* from
            k_task_info t
        where del_flag = 0
    </select>

    <select id="selectOneRecord"  resultMap="TaskInfoResult">
        select t.* from
            k_task_info t
        where del_flag = 0
            and id = #{id}
            and task_type = #{taskType}
            and rownum = 1
    </select>

</mapper>