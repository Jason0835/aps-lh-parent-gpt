<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zlt.aps.tm.mapper.TmScheduleResultExplainMapper">

    <update id="logicDeleteByFactoryCodeAndScheduleDate">
        update T_TM_SCHEDULE_RESULT_EXPLAIN explain
        inner join T_TM_SCHEDULE_RESULT result
            on explain.RESULT_ID = result.ID
           and result.IS_DELETE = 0
        set explain.IS_DELETE = 1,
            explain.UPDATE_TIME = now()
        where explain.IS_DELETE = 0
          and result.FACTORY_CODE = #{factoryCode}
          and result.SCHEDULE_DATE = #{scheduleDate}
    </update>

</mapper>
