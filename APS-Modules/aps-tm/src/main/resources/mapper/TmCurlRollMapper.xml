<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zlt.aps.tm.mapper.TmCurlRollMapper">

    <select id="listCurlRoll" parameterType="com.zlt.aps.tm.api.domain.entity.TmCurlRoll"
            resultType="com.zlt.aps.tm.api.domain.entity.TmCurlRoll">
        select *
        from T_TM_CURL_ROLL
        where DEL_FLAG = 0
        <if test="treadCode != null and treadCode != ''">
            and INSTR(TREAD_CODE, #{treadCode,jdbcType=VARCHAR}) > 0
        </if>
        <if test="queryCode != null and queryCode != ''">
            and TREAD_CODE = #{queryCode,jdbcType=VARCHAR}
        </if>
        <if test="params != null and params.codeList != null and params.codeList.size() > 0">
            AND tread_code IN
            <foreach collection="params.codeList" item="code" separator="," open="(" close=")">
                #{code}
            </foreach>
        </if>
        <if test="orderStr == null">
            order by CREATE_TIME desc
        </if>
        <if test="orderStr != null">
            order by ${orderStr}
        </if>
    </select>

    <insert id="mergeSql" parameterType="list">
        INSERT INTO T_TM_CURL_ROLL (
        TREAD_CODE, CURL_LENGTH,
        create_by, create_time, update_by, update_time, remark, del_flag
        ) VALUES
        <foreach collection="list" item="tmCurlRoll" separator=",">
            (
            #{tmCurlRoll.treadCode,jdbcType=VARCHAR},
            #{tmCurlRoll.curlLength,jdbcType=DECIMAL},
            #{tmCurlRoll.createBy,jdbcType=VARCHAR},
            NOW(),
            #{tmCurlRoll.updateBy,jdbcType=VARCHAR},
            NOW(),
            #{tmCurlRoll.remark,jdbcType=VARCHAR},
            '0'
            )
        </foreach>
        ON DUPLICATE KEY UPDATE
        CURL_LENGTH = VALUES(CURL_LENGTH),
        update_by = VALUES(create_by),
        update_time = NOW(),
        remark = VALUES(remark)
    </insert>

</mapper>
