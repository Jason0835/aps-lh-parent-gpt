<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zlt.aps.cx.mapper.CxSpecColorMapper">

    <resultMap type="com.zlt.aps.cx.api.domain.dto.CxSpecColorDto" id="CxSpecColorResult">
        <result property="id" column="id"/>
        <result property="specDesc" column="spec_desc"/>
        <result property="colorType" column="color_type"/>
        <result property="colorCode" column="color_code"/>
        <result property="status" column="status"/>
        <result property="remark" column="remark"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="selectCxSpecColorVo">
        select id, spec_desc, color_type, color_code, status, remark, del_flag, create_by, create_time, update_by, update_time from t_cx_spec_color
    </sql>

    <select id="selectCxSpecColorList" parameterType="com.zlt.aps.cx.entity.CxSpecColor"
            resultMap="CxSpecColorResult">
        <include refid="selectCxSpecColorVo"/>
        where del_flag = '0'
        <if test="specDesc != null and specDesc != ''">and spec_desc like concat(concat('%', #{specDesc}), '%')</if>
        <if test="colorType != null and colorType != ''">and color_type = #{colorType}</if>
        <if test="colorCode != null and colorCode != ''">and INSTR(color_code, #{colorCode}) > 0</if>
        <if test="status != null  and status != ''">and status = #{status}</if>
        <if test="orderStr == null">
            order by create_time desc
        </if>
        <if test="orderStr != null">
            order by ${orderStr}
        </if>
    </select>

    <select id="selectCxSpecColorById" parameterType="Long" resultMap="CxSpecColorResult">
        <include refid="selectCxSpecColorVo"/>
        where id = #{id}
    </select>

    <select id="checkCxSpecColorUnique" parameterType="com.zlt.aps.cx.entity.CxSpecColor"
            resultType="int">
        select count(id)
        from t_cx_spec_color
        where del_flag = 0
        <if test="specDesc != null and specDesc != ''">and spec_desc = #{specDesc}</if>
        <if test="colorType != null and colorType != ''">and color_type = #{colorType}</if>
        <if test="colorCode != null and colorCode != ''">and color_code = #{colorCode}</if>
        <if test="status != null  and status != ''">and status = #{status}</if>
        <if test="id != null and id != ''">and id != #{id}</if>
    </select>

    <insert id="insertCxSpecColor" parameterType="com.zlt.aps.cx.entity.CxSpecColor">
        <selectKey keyProperty="id" resultType="long" order="BEFORE">
            SELECT SEQ_PUBLIC.NEXTVAL as id
            FROM DUAL
        </selectKey>
        insert into t_cx_spec_color
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="specDesc != null">spec_desc,</if>
            <if test="colorType != null">color_type,</if>
            <if test="colorCode != null">color_code,</if>
            <if test="status != null">status,</if>
            <if test="remark != null">remark,</if>
            <if test="delFlag != null">del_flag,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id},</if>
            <if test="specDesc != null">#{specDesc},</if>
            <if test="colorType != null">#{colorType},</if>
            <if test="colorCode != null">#{colorCode},</if>
            <if test="status != null">#{status},</if>
            <if test="remark != null">#{remark},</if>
            <if test="delFlag != null">#{delFlag},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
        </trim>
    </insert>

    <update id="updateCxSpecColor" parameterType="com.zlt.aps.cx.entity.CxSpecColor">
        update t_cx_spec_color
        <trim prefix="SET" suffixOverrides=",">
            <if test="specDesc != null">spec_desc = #{specDesc},</if>
            <if test="colorType != null">color_type = #{colorType},</if>
            <if test="colorCode != null">color_code = #{colorCode},</if>
            <if test="status != null">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="delFlag != null">del_flag = #{delFlag},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteCxSpecColorById" parameterType="Long">
        update t_cx_spec_color
        set del_flag='1'
        where id = #{id}
    </delete>

    <delete id="deleteCxSpecColorByIds" parameterType="String">
        update t_cx_spec_color set del_flag='1' where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <update id="mergeSql" parameterType="list">
        merge into t_cx_spec_color a USING (
        <foreach collection="list" item="specColor" separator=" union ">
            select
            #{specColor.specDesc,jdbcType=VARCHAR} spec_desc,
            #{specColor.colorType,jdbcType=VARCHAR} color_type,
            #{specColor.colorCode,jdbcType=VARCHAR} color_code,
            #{specColor.status,jdbcType=VARCHAR} status,

            #{specColor.createBy,jdbcType=VARCHAR} create_by,
            #{specColor.createTime,jdbcType=DATE} create_time,
            #{specColor.updateBy,jdbcType=VARCHAR} update_by,
            #{specColor.updateTime,jdbcType=DATE} update_time,
            #{specColor.remark,jdbcType=VARCHAR} remark
            from dual
        </foreach>
        ) b ON (
        a.spec_desc = b.spec_desc
        AND a.del_flag = '0' )
        WHEN MATCHED THEN
        UPDATE SET
        a.color_type = b.color_type,
        a.color_code = b.color_code,
        a.status = b.status,
        a.update_by = b.create_by,
        a.update_time = sysdate,
        a.remark = b.remark
        WHEN NOT MATCHED THEN
        INSERT (a.id, a.spec_desc, a.color_type, a.color_code, a.status, a.create_by, a.create_time,a.remark, a.del_flag)
        values (SEQ_PUBLIC.NEXTVAL, b.spec_desc, b.color_type, b.color_code, b.status,b.create_by, sysdate,b.remark, '0')
    </update>
</mapper>