<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zlt.aps.tq.mapper.TqToolingMapper">

    <select id="listTooling" parameterType="com.zlt.aps.tq.api.domain.entity.TqTooling"
            resultType="com.zlt.aps.tq.api.domain.entity.TqTooling">
        select * from (
            select ID, TOOLING_CODE, TOOLING_NAME, TOTAL_QTY,
                   REMARK, IS_DELETE, CREATE_BY, CREATE_TIME, UPDATE_BY, UPDATE_TIME
            from T_TQ_TOOLING
            where IS_DELETE = '0'
            <if test="toolingCode != null and toolingCode != ''">
                and TOOLING_CODE like concat('%', #{toolingCode}, '%')
            </if>
            order by CREATE_TIME desc
        ) t
    </select>

    <select id="checkUnique" parameterType="com.zlt.aps.tq.api.domain.entity.TqTooling" resultType="int">
        SELECT count(1)
        FROM T_TQ_TOOLING
        where IS_DELETE = '0'
        and TOOLING_CODE = #{toolingCode}
        <if test="id != null">and ID != #{id}</if>
    </select>

    <insert id="mergeSql" parameterType="list">
        INSERT INTO T_TQ_TOOLING (
            tooling_code, tooling_name, total_qty, remark,
            create_by, create_time, update_by, update_time, is_delete
        )
        VALUES
        <foreach collection="list" item="Tooling" separator=",">
            (
            #{Tooling.toolingCode,jdbcType=VARCHAR},
            #{Tooling.toolingName,jdbcType=VARCHAR},
            #{Tooling.totalQty,jdbcType=INTEGER},
            #{Tooling.remark,jdbcType=VARCHAR},
            #{Tooling.createBy,jdbcType=VARCHAR},
            NOW(),
            #{Tooling.updateBy,jdbcType=VARCHAR},
            NOW(),
            '0'
            )
        </foreach>
        ON DUPLICATE KEY UPDATE
        tooling_name = VALUES(tooling_name),
        total_qty = VALUES(total_qty),
        update_by = VALUES(create_by),
        update_time = NOW(),
        remark = VALUES(remark)
    </insert>

    <update id="deleteAllTooling">
        update T_TQ_TOOLING set IS_DELETE = '1' where IS_DELETE = '0'
    </update>

</mapper>
