<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zlt.aps.tq.mapper.TqToolingCartCapacityMapper">

    <select id="listToolingCartCapacity" parameterType="com.zlt.aps.tq.api.domain.entity.TqToolingCartCapacity"
            resultType="com.zlt.aps.tq.api.domain.entity.TqToolingCartCapacity">
        select * from (
            select ID, BEAD_CODE, CART_CAPACITY,
                   REMARK, IS_DELETE, CREATE_BY, CREATE_TIME, UPDATE_BY, UPDATE_TIME
            from T_TQ_TOOLING_CART_CAPACITY
            where IS_DELETE = '0'
            <if test="beadCode != null and beadCode != ''">
                and BEAD_CODE like concat('%', #{beadCode}, '%')
            </if>
            order by CREATE_TIME desc
        ) t
    </select>

    <insert id="mergeSql" parameterType="list">
        INSERT INTO T_TQ_TOOLING_CART_CAPACITY (
            bead_code, cart_capacity, remark,
            create_by, create_time, update_by, update_time, is_delete
        )
        VALUES
        <foreach collection="list" item="cartCapacity" separator=",">
            (
            #{cartCapacity.beadCode,jdbcType=VARCHAR},
            #{cartCapacity.cartCapacity,jdbcType=INTEGER},
            #{cartCapacity.remark,jdbcType=VARCHAR},
            #{cartCapacity.createBy,jdbcType=VARCHAR},
            NOW(),
            #{cartCapacity.updateBy,jdbcType=VARCHAR},
            NOW(),
            '0'
            )
        </foreach>
        ON DUPLICATE KEY UPDATE
        cart_capacity = VALUES(cart_capacity),
        update_by = VALUES(update_by),
        update_time = NOW(),
        remark = VALUES(remark),
        is_delete = '0'
    </insert>

    <update id="deleteAllToolingCartCapacity">
        update T_TQ_TOOLING_CART_CAPACITY set IS_DELETE = '1' where IS_DELETE = '0'
    </update>

</mapper>
