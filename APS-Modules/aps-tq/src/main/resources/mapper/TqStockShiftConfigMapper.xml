<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zlt.aps.tq.mapper.TqStockShiftConfigMapper">

    <resultMap type="com.zlt.aps.tq.api.domain.entity.TqStockShiftConfig" id="TqStockShiftConfigResult">
        <id property="id" column="ID"/>
        <result property="factoryCode" column="FACTORY_CODE"/>
        <result property="minMachineQty" column="MIN_MACHINE_QTY"/>
        <result property="maxMachineQty" column="MAX_MACHINE_QTY"/>
        <result property="depthClassQty" column="DEPTH_CLASS_QTY"/>
        <result property="remark" column="REMARK"/>
        <result property="createBy" column="CREATE_BY"/>
        <result property="createTime" column="CREATE_TIME"/>
        <result property="updateBy" column="UPDATE_BY"/>
        <result property="updateTime" column="UPDATE_TIME"/>
    </resultMap>

    <select id="selectStockShiftConfigList" parameterType="com.zlt.aps.tq.api.domain.entity.TqStockShiftConfig"
            resultMap="TqStockShiftConfigResult">
        select ID, FACTORY_CODE, MIN_MACHINE_QTY, MAX_MACHINE_QTY, DEPTH_CLASS_QTY, REMARK,
               CREATE_BY, CREATE_TIME, UPDATE_BY, UPDATE_TIME
        from T_TQ_STOCK_SHIFT_CONFIG
        <where>
            IS_DELETE = 0
            <if test="factoryCode != null and factoryCode != ''">and FACTORY_CODE = #{factoryCode}</if>
            <if test="minMachineQty != null">and MIN_MACHINE_QTY = #{minMachineQty}</if>
        </where>
        order by FACTORY_CODE, MIN_MACHINE_QTY
    </select>

    <insert id="mergeSql" parameterType="list">
        INSERT INTO T_TQ_STOCK_SHIFT_CONFIG (
            FACTORY_CODE, MIN_MACHINE_QTY, MAX_MACHINE_QTY, DEPTH_CLASS_QTY, REMARK, CREATE_BY, CREATE_TIME, IS_DELETE
        )
        VALUES
        <foreach collection="list" item="item" separator=",">
            (
            #{item.factoryCode,jdbcType=VARCHAR},
            #{item.minMachineQty,jdbcType=INTEGER},
            #{item.maxMachineQty,jdbcType=INTEGER},
            #{item.depthClassQty,jdbcType=DECIMAL},
            #{item.remark,jdbcType=VARCHAR},
            #{item.createBy,jdbcType=VARCHAR},
            NOW(),
            0
            )
        </foreach>
        ON DUPLICATE KEY UPDATE
        DEPTH_CLASS_QTY = VALUES(DEPTH_CLASS_QTY),
        MAX_MACHINE_QTY = VALUES(MAX_MACHINE_QTY),
        UPDATE_BY = VALUES(CREATE_BY),
        UPDATE_TIME = NOW(),
        REMARK = VALUES(REMARK)
    </insert>

</mapper>
