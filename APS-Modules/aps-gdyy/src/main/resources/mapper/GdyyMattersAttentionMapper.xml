<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zlt.aps.gdyy.mapper.GdyyMattersAttentionMapper">

    <!-- 通用查询映射结果 -->
    <select id="listGwyyMattersAttention" parameterType="com.zlt.aps.gdyy.api.domain.dto.GdyyMattersAttentionDto"
            resultType="com.zlt.aps.gdyy.api.domain.dto.GdyyMattersAttentionDto">
        select *
        from T_GDYY_NOTE
        where DEL_FLAG = 0
        <if test="bigRollCode != null and bigRollCode != ''">
            and INSTR(BIG_ROLL_CODE, #{bigRollCode,jdbcType=VARCHAR}) > 0
        </if>
        <if test="orderStr == null">
            order by CREATE_TIME desc
        </if>
        <if test="orderStr != null">
            order by ${orderStr}
        </if>
    </select>
    <insert id="mergeSql" parameterType="list">
        INSERT INTO T_GDYY_NOTE (
        big_roll_code, notes, start_time, end_time, status,
        remark, create_by, create_time, update_by, update_time, del_flag
        ) VALUES
        <foreach collection="list" item="Attention" separator=",">
            (
            #{Attention.bigRollCode,jdbcType=VARCHAR},
            #{Attention.notes,jdbcType=VARCHAR},
            #{Attention.startTime,jdbcType=DATE},
            #{Attention.endTime,jdbcType=DATE},
            #{Attention.status,jdbcType=VARCHAR},
            #{Attention.remark,jdbcType=VARCHAR},
            #{Attention.createBy,jdbcType=VARCHAR},
            NOW(),
            #{Attention.updateBy,jdbcType=VARCHAR},
            NOW(),
            '0'
            )
        </foreach>
        ON DUPLICATE KEY UPDATE
        notes = VALUES(notes),
        start_time = VALUES(start_time),
        end_time = VALUES(end_time),
        status = VALUES(status),
        update_by = VALUES(create_by),
        update_time = NOW(),
        remark = VALUES(remark)
    </insert>
</mapper>
