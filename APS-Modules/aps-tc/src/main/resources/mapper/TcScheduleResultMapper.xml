<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zlt.aps.tc.mapper.TcScheduleResultMapper">
    <!-- 管理员修改发布状态前锁定目标结果，保证校验和写入基于同一份数据库状态。 -->
    <select id="selectBatchIdsForUpdate" resultType="com.zlt.aps.tc.api.domain.entity.TcScheduleResult">
        select *
        from T_TC_SCHEDULE_RESULT
        where ID in
        <foreach item="id" collection="ids" open="(" separator="," close=")">
            #{id}
        </foreach>
        and IS_DELETE = 0
        order by ID
        for update
    </select>

    <select id="isReleasingOrTimeoutByIds" parameterType="string" resultType="int">
        select count(1) from T_TC_SCHEDULE_RESULT
        where is_delete = 0 and (RELEASE_STATUS = '3' or RELEASE_STATUS = '4')
        and id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <!-- 旧批次逻辑删除：仅删除未发布(0)/发布失败(2)/超时失败(4)的可覆盖批次，
         防御性排除已发布(1)/发布中(3)/待发布(5)，避免绕过 Step2 覆盖校验的程序调用误删已发布数据。
         正常入口下 Step2 已保证不存在 1/3/5 批次，此过滤为兜底防护。 -->
    <update id="logicDeleteByFactoryCodeAndScheduleDate">
        update T_TC_SCHEDULE_RESULT
        set IS_DELETE = 1,
            UPDATE_TIME = now()
        where IS_DELETE = 0
          and FACTORY_CODE = #{factoryCode}
          and SCHEDULE_DATE = #{scheduleDate}
          and RELEASE_STATUS not in ('1', '3', '5')
    </update>

    <!-- Excel 导入短事务使用：锁定当前有效批次全部结果，保证版本和顺序校验基于同一快照。 -->
    <select id="selectScopeForUpdate" resultType="com.zlt.aps.tc.api.domain.entity.TcScheduleResult">
        SELECT *
        FROM T_TC_SCHEDULE_RESULT
        WHERE FACTORY_CODE = #{factoryCode}
          AND SCHEDULE_DATE = #{scheduleDate}
          AND BATCH_NO = #{batchNo}
        ORDER BY ID
        FOR UPDATE
    </select>

</mapper>
