<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zlt.aps.tc.mapper.TcScheduleResultExplainMapper">

    <update id="logicDeleteByFactoryCodeAndScheduleDate">
        update T_TC_SCHEDULE_RESULT_EXPLAIN
        set IS_DELETE = 1,
            UPDATE_TIME = now()
        where IS_DELETE = 0
          and FACTORY_CODE = #{factoryCode}
          and SCHEDULE_DATE = #{scheduleDate}
    </update>

    <update id="logicDeleteByFactoryCodeAndBatchNos">
        update T_TC_SCHEDULE_RESULT_EXPLAIN
        set IS_DELETE = 1,
            UPDATE_TIME = now()
        where IS_DELETE = 0
          and FACTORY_CODE = #{factoryCode}
          and BATCH_NO in
        <foreach item="batchNo" collection="batchNoList" open="(" separator="," close=")">
            #{batchNo}
        </foreach>
    </update>

</mapper>
