<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zlt.aps.cd90.mapper.Cd90BigRollMapper">

    <select id="listBigRoll" parameterType="com.zlt.aps.cd90.api.domain.dto.Cd90BigRollDto"
            resultType="com.zlt.aps.cd90.api.domain.dto.Cd90BigRollDto">
        select *
        from T_CD90_BIG_ROLL
        where DEL_FLAG = 0
        <if test="bigRollCode != null and bigRollCode != ''">
            and INSTR(BIG_ROLL_CODE, #{bigRollCode,jdbcType=VARCHAR}) > 0
        </if>
        <if test="orderStr == null">
            order by CREATE_TIME desc
        </if>
        <if test="orderStr != null">
            order by ${orderStr}
        </if>
    </select>

    <update id="mergeSql" parameterType="list">
        merge into T_CD90_BIG_ROLL a USING (
        <foreach collection="list" item="Cd90BigRoll" separator=" union ">
            select
                #{Cd90BigRoll.bigRollCode,jdbcType=VARCHAR} BIG_ROLL_CODE,
                #{Cd90BigRoll.clothLength,jdbcType=DECIMAL} CLOTH_LENGTH,
                #{Cd90BigRoll.convertProduceNum,jdbcType=DECIMAL} CONVERT_PRODUCE_NUM,
                #{Cd90BigRoll.actClothLength,jdbcType=DECIMAL} ACT_CLOTH_LENGTH,

                #{Cd90BigRoll.createBy,jdbcType=VARCHAR} create_by,
                #{Cd90BigRoll.createTime,jdbcType=DATE} create_time,
                #{Cd90BigRoll.updateBy,jdbcType=VARCHAR} update_by,
                #{Cd90BigRoll.updateTime,jdbcType=DATE} update_time,
                #{Cd90BigRoll.remark,jdbcType=VARCHAR} remark
            from dual
        </foreach>
        ) b ON (
            a.BIG_ROLL_CODE = b.BIG_ROLL_CODE
            AND a.del_flag = '0'
        )WHEN MATCHED THEN
            UPDATE SET
                a.CLOTH_LENGTH   = b.CLOTH_LENGTH,
                a.CONVERT_PRODUCE_NUM   = b.CONVERT_PRODUCE_NUM,
                a.ACT_CLOTH_LENGTH   = b.ACT_CLOTH_LENGTH,

                a.update_by   = b.create_by,
                a.update_time = sysdate,
                a.remark      = b.remark
        WHEN NOT MATCHED THEN
            INSERT (
                a.id          ,

                a.BIG_ROLL_CODE  ,
                a.CLOTH_LENGTH  ,
                a.CONVERT_PRODUCE_NUM   ,
                a.ACT_CLOTH_LENGTH   ,

                a.create_by   ,
                a.create_time ,
                a.remark,
                a.del_flag
            ) values(
                SEQ_PUBLIC.NEXTVAL,

                b.BIG_ROLL_CODE  ,
                b.CLOTH_LENGTH  ,
                b.CONVERT_PRODUCE_NUM   ,
                b.ACT_CLOTH_LENGTH   ,

                b.create_by,
                sysdate,
                b.remark,
                '0'
            )
    </update>

</mapper>
