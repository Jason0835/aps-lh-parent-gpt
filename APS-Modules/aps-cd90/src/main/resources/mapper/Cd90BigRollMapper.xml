<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zlt.aps.cd90.mapper.Cd90BigRollMapper">

    <select id="listBigRoll" parameterType="com.zlt.aps.cd90.api.domain.dto.Cd90BigRollDto"
            resultType="com.zlt.aps.cd90.api.domain.dto.Cd90BigRollDto">
        select *
        from T_CD90_BIG_ROLL
        where DEL_FLAG = 0
        <if test="bigRollCode != null and bigRollCode != ''">
            and INSTR(BIG_ROLL_CODE, #{bigRollCode,jdbcType=VARCHAR}) > 0
        </if>
        <if test="orderStr == null">
            order by CREATE_TIME desc
        </if>
        <if test="orderStr != null">
            order by ${orderStr}
        </if>
    </select>

    <insert id="mergeSql" parameterType="list">
        INSERT INTO T_CD90_BIG_ROLL (
        BIG_ROLL_CODE, CLOTH_LENGTH, CONVERT_PRODUCE_NUM, ACT_CLOTH_LENGTH,
        create_by, create_time, update_by, update_time, remark, del_flag
        ) VALUES
        <foreach collection="list" item="Cd90BigRoll" separator=",">
            (
            #{Cd90BigRoll.bigRollCode,jdbcType=VARCHAR},
            #{Cd90BigRoll.clothLength,jdbcType=DECIMAL},
            #{Cd90BigRoll.convertProduceNum,jdbcType=DECIMAL},
            #{Cd90BigRoll.actClothLength,jdbcType=DECIMAL},
            #{Cd90BigRoll.createBy,jdbcType=VARCHAR},
            NOW(),
            #{Cd90BigRoll.updateBy,jdbcType=VARCHAR},
            NOW(),
            #{Cd90BigRoll.remark,jdbcType=VARCHAR},
            '0'
            )
        </foreach>
        ON DUPLICATE KEY UPDATE
        CLOTH_LENGTH = VALUES(CLOTH_LENGTH),
        CONVERT_PRODUCE_NUM = VALUES(CONVERT_PRODUCE_NUM),
        ACT_CLOTH_LENGTH = VALUES(ACT_CLOTH_LENGTH),
        update_by = VALUES(create_by),
        update_time = NOW(),
        remark = VALUES(remark)
    </insert>

</mapper>
