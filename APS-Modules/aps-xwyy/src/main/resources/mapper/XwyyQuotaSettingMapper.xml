<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zlt.aps.xwyy.mapper.XwyyQuotaSettingMapper">

    <resultMap type="com.zlt.aps.xwyy.api.domain.dto.XwyyQuotaSettingDto" id="quotaSettingResult">
        <result property="id" column="id"/>
        <result property="bigRollCode" column="big_roll_code"/>
        <result property="machineId" column="machine_id"/>
        <result property="machineName" column="machine_name"/>
        <result property="quota" column="quota"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="delFlag" column="del_flag"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="selectXwyyQuotaSettingVo">
        select id, big_roll_code, machine_id, quota, create_by, create_time, update_by, update_time, del_flag, remark from t_xwyy_quota_setting
    </sql>

    <select id="selectQuotaSettingList" parameterType="com.zlt.aps.xwyy.entity.XwyyQuotaSetting"
            resultMap="quotaSettingResult">
        select * from (
        SELECT
        a.*,
        b.MACHINE_NAME
        FROM
        T_XWYY_QUOTA_SETTING a
        LEFT JOIN T_XWYY_MACHINE_INFO b
        ON a.MACHINE_ID = b.ID
        WHERE
        a.del_flag = '0'
        <if test="bigRollCode != null  and bigRollCode != ''">
            and INSTR(big_roll_code, #{bigRollCode,jdbcType=VARCHAR}) > 0</if>
        <if test="machineId != null ">and a.machine_id = #{machineId}</if>
        <if test="quota != null ">and a.quota = #{quota}</if>
        <if test="orderStr == null">
            order by a.CREATE_TIME desc
        </if>
        ) t
        <if test="orderStr != null">
            order by ${orderStr}
        </if>

    </select>

    <select id="selectQuotaSettingById" parameterType="Long" resultMap="quotaSettingResult">
        <include refid="selectXwyyQuotaSettingVo"/>
        where id = #{id}
    </select>

    <select id="checkUnique" parameterType="com.zlt.aps.xwyy.entity.XwyyQuotaSetting" resultMap="quotaSettingResult">
        <include refid="selectXwyyQuotaSettingVo"/>
        where del_flag = '0'
        <if test="id != null and id != ''">and id != #{id}</if>
        <if test="machineId == null or machineId == ''">and machine_id is null</if>
        <if test="machineId != null and machineId != ''">and machine_id = #{machineId}</if>
        <if test="bigRollCode == null or bigRollCode == ''">and big_roll_code is null</if>
        <if test="bigRollCode != null  and bigRollCode != ''">and big_roll_code = #{bigRollCode}</if>
    </select>

    <delete id="deleteQuotaSettingByIds" parameterType="long">
        update t_xwyy_quota_setting set del_flag = NULL where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="selectExistByList" parameterType="list" resultMap="quotaSettingResult">
        SELECT *
        FROM t_xwyy_quota_setting
        where (
        big_roll_code,
        machine_id
        ) in (
        <foreach collection="list" item="XwyyQuotaSetting" separator=",">
            (
            <if test="XwyyQuotaSetting.bigRollCode == null or XwyyQuotaSetting.bigRollCode == ''">null</if>
            <if test="XwyyQuotaSetting.bigRollCode != null and XwyyQuotaSetting.bigRollCode != ''">
                #{XwyyQuotaSetting.bigRollCode}
            </if>
            ,
            <if test="XwyyQuotaSetting.machineId == null or XwyyQuotaSetting.machineId == ''">null</if>
            <if test="XwyyQuotaSetting.machineId != null and XwyyQuotaSetting.machineId != ''">
                #{XwyyQuotaSetting.machineId}
            </if>
            )
        </foreach>
        )
    </select>

    <select id="selectByBigRollCodeAndMachineId" parameterType="com.zlt.aps.xwyy.api.domain.dto.XwyyQuotaSettingDto"
            resultMap="quotaSettingResult">
        <include refid="selectXwyyQuotaSettingVo"/>
        where del_flag = '0'
        <if test="id != null and id != ''">and id != #{id}</if>
        <if test="machineId == null or machineId == ''">and machine_id = is null</if>
        <if test="machineId != null and machineId != ''">and machine_id = #{machineId}</if>
        <if test="bigRollCode == null or bigRollCode == ''">and big_roll_code is null</if>
        <if test="bigRollCode != null  and bigRollCode != ''">and big_roll_code = #{bigRollCode}</if>
    </select>

    <insert id="insertList" parameterType="list">
        insert into t_xwyy_quota_setting
        (id, big_roll_code, machine_id, quota, create_by, create_time, del_flag, remark)
        VALUES
        <foreach collection="list" item="XwyyQuotaSetting" separator=",">
            (NULL,
            #{XwyyQuotaSetting.bigRollCode,jdbcType=VARCHAR},#{XwyyQuotaSetting.machineId,jdbcType=DOUBLE},#{XwyyQuotaSetting.quota,jdbcType=DOUBLE},#{XwyyQuotaSetting.createBy},#{XwyyQuotaSetting.createTime},
            #{XwyyQuotaSetting.delFlag},#{XwyyQuotaSetting.remark,jdbcType=VARCHAR}
            )
        </foreach>
    </insert>

    <delete id="deleteById">
        delete from t_xwyy_quota_setting
        where id in
        <foreach collection="list" item="XwyyQuotaSetting" open="(" close=")" separator=",">
            #{XwyyQuotaSetting.id}
        </foreach>
    </delete>

    <update id="updateList" parameterType="list">
        update t_xwyy_quota_setting
        <trim prefix="SET" suffixOverrides=",">
            <if test="id != null and id != ''">id = #{id}</if>
            <if test="bigRollCode != null and bigRollCode != ''">big_roll_code = #{bigRollCode}</if>
            <if test="machineId != null and machineId != ''">machine_id = #{machineId}</if>
            <if test="quota != null and quota != ''">quota = #{quota}</if>
            <if test="createBy != null and createBy != ''">create_by = #{createBy}</if>
            <if test="createTime != null and createTime != ''">create_time = #{createTime}</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy}</if>
            <if test="updateTime != null and updateTime != ''">update_time = #{updateTime}</if>
            <if test="remark != null and remark != ''">remark = #{remark}</if>
        </trim>
    </update>

    <insert id="mergeSql" parameterType="list">
        INSERT INTO t_xwyy_quota_setting (
        big_roll_code, machine_id, quota, create_by, create_time, remark, del_flag
        )
        VALUES
        <foreach collection="list" item="XwyyQuotaSetting" separator=",">
            (
            #{XwyyQuotaSetting.bigRollCode,jdbcType=VARCHAR},
            #{XwyyQuotaSetting.machineId,jdbcType=DOUBLE},
            #{XwyyQuotaSetting.quota,jdbcType=DOUBLE},
            #{XwyyQuotaSetting.createBy,jdbcType=VARCHAR},
            NOW(),
            #{XwyyQuotaSetting.remark,jdbcType=VARCHAR},
            '0'
            )
        </foreach>
        ON DUPLICATE KEY UPDATE
        quota = VALUES(quota),
        update_by = VALUES(create_by),
        update_time = NOW(),
        remark = VALUES(remark)
    </insert>
</mapper>