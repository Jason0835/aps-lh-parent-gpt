<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zlt.aps.monthplan.adjust.mapper.MpAdjustStructureLogEntityMapper">

    <delete id="deleteAdjustLogByVersion" parameterType="String">
        DELETE FROM T_MP_ADJUST_STRUCTURE_LOG
        WHERE FACTORY_CODE = #{factoryCode} AND YEAR= #{year} AND MONTH= #{month}
        AND ADJ_VERSION= #{version} AND STRUCTURE_NAME = #{structureName}
    </delete>

</mapper>