<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zlt.aps.mp.adjust.mapper.MpAdjustStructureOutEntityMapper">

    <select id="getVersionList" resultType="com.zlt.aps.monthplan.api.domain.entity.MpAdjustStructureIn">
        SELECT DISTINCT
        VERSION
        FROM
        T_MP_ADJUST_STRUCTURE_OUT
        WHERE
        IS_DELETE = 0 AND VERSION IS NOT NULL
        <if test="factoryCode != null and factoryCode != ''">AND FACTORY_CODE = #{factoryCode}</if>
        <if test="year != null and year != ''">AND YEAR = #{year}</if>
        <if test="month != null and month != ''">AND MONTH = #{month}</if>
        ORDER BY VERSION DESC
    </select>

</mapper>