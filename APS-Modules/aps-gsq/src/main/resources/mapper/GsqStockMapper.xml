<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zlt.aps.gsq.mapper.GsqStockMapper">

    <!-- 校验"库存日期+钢丝圈代码"组合唯一性 -->
    <select id="checkUnique" parameterType="com.zlt.aps.gsq.api.domain.entity.GsqStock" resultType="int">
        SELECT count(1)
        FROM T_GSQ_STOCK
        where IS_DELETE = '0'
        and STOCK_DATE = #{stockDate}
        and STEEL_RING_CODE = #{steelRingCode}
        <if test="id != null">and ID != #{id}</if>
    </select>

    <!-- 批量合并保存（存在则更新，否则新增），用于导入场景 -->
    <insert id="mergeSql" parameterType="list">
        INSERT INTO T_GSQ_STOCK (
            STOCK_DATE, STEEL_RING_CODE, STOCK_NUM, MODIFY_NUM, BAD_NUM, REMARK,
            CREATE_BY, CREATE_TIME, UPDATE_BY, UPDATE_TIME, IS_DELETE
        )
        VALUES
        <foreach collection="list" item="stock" separator=",">
            (
            #{stock.stockDate,jdbcType=DATE},
            #{stock.steelRingCode,jdbcType=VARCHAR},
            #{stock.stockNum,jdbcType=DECIMAL},
            #{stock.modifyNum,jdbcType=DECIMAL},
            #{stock.badNum,jdbcType=DECIMAL},
            #{stock.remark,jdbcType=VARCHAR},
            #{stock.createBy,jdbcType=VARCHAR},
            NOW(),
            #{stock.updateBy,jdbcType=VARCHAR},
            NOW(),
            '0'
            )
        </foreach>
        ON DUPLICATE KEY UPDATE
        STOCK_NUM = VALUES(STOCK_NUM),
        MODIFY_NUM = VALUES(MODIFY_NUM),
        BAD_NUM = VALUES(BAD_NUM),
        UPDATE_BY = VALUES(CREATE_BY),
        UPDATE_TIME = NOW(),
        REMARK = VALUES(REMARK)
    </insert>

</mapper>
