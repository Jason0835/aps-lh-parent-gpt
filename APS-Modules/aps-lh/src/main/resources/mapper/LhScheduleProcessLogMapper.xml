<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zlt.aps.lh.mapper.LhScheduleProcessLogMapper">

    <insert id="insertBatch">
        INSERT INTO T_LH_SCHEDULE_PROCESS_LOG (
            BATCH_NO, ORDER_NO, TITLE, BUSI_CODE, LOG_DETAIL,
            CREATE_BY, CREATE_TIME, UPDATE_BY, UPDATE_TIME, REMARK, IS_DELETE
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.batchNo}, #{item.orderNo}, #{item.title}, #{item.busiCode}, #{item.logDetail},
                #{item.createBy}, #{item.createTime}, #{item.updateBy}, #{item.updateTime}, #{item.remark}, #{item.isDelete}
            )
        </foreach>
    </insert>

</mapper>
