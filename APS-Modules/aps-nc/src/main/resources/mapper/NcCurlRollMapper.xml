<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zlt.aps.nc.mapper.NcCurlRollMapper">

    <select id="listCurlRoll" parameterType="com.zlt.aps.nc.api.domain.entity.NcCurlRoll"
            resultType="com.zlt.aps.nc.api.domain.entity.NcCurlRoll">
        select *
        from T_NC_CURL_ROLL
        where DEL_FLAG = 0
        <if test="liningCode != null and liningCode != ''">
            and INSTR(LINING_CODE, #{liningCode,jdbcType=VARCHAR}) > 0
        </if>
        <if test="queryCode != null and queryCode != ''">
            and LINING_CODE = #{queryCode,jdbcType=VARCHAR}
        </if>
        <if test="params != null and params.codeList != null and params.codeList.size() > 0">
            AND LINING_CODE IN
            <foreach collection="params.codeList" item="code" separator="," open="(" close=")">
                #{code}
            </foreach>
        </if>
        <if test="orderStr == null">
            order by CREATE_TIME desc
        </if>
        <if test="orderStr != null">
            order by ${orderStr}
        </if>
    </select>

    <insert id="mergeSql" parameterType="list">
        INSERT INTO T_NC_CURL_ROLL (
        LINING_CODE, CURL_LENGTH,
        create_by, create_time, update_by, update_time, remark, del_flag
        ) VALUES
        <foreach collection="list" item="ncCurlRoll" separator=",">
            (
            #{ncCurlRoll.liningCode,jdbcType=VARCHAR},
            #{ncCurlRoll.curlLength,jdbcType=DECIMAL},
            #{ncCurlRoll.createBy,jdbcType=VARCHAR},
            NOW(),
            #{ncCurlRoll.updateBy,jdbcType=VARCHAR},
            NOW(),
            #{ncCurlRoll.remark,jdbcType=VARCHAR},
            '0'
            )
        </foreach>
        ON DUPLICATE KEY UPDATE
        CURL_LENGTH = VALUES(CURL_LENGTH),
        update_by = VALUES(create_by),
        update_time = NOW(),
        remark = VALUES(remark)
    </insert>

</mapper>
