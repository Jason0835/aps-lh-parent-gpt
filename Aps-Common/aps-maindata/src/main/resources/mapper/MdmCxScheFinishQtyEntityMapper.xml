<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zlt.aps.maindata.mapper.MdmCxScheFinishQtyEntityMapper">

    <select id="selectByUniqueKeyList" resultType="com.zlt.aps.mp.api.domain.entity.MdmCxScheFinishQty">
        SELECT
            ID,
            ORDER_NO,
            SCHEDULE_DATE,
            CX_MACHINE_CODE,
            FACTORY_CODE
        FROM T_MDM_CX_SCHE_FINISH_QTY
        WHERE (ORDER_NO, SCHEDULE_DATE, CX_MACHINE_CODE, FACTORY_CODE) IN
        <foreach collection="list" item="item" open="(" separator="," close=")">
            (#{item.orderNo}, #{item.scheduleDate}, #{item.cxMachineCode}, #{item.factoryCode})
        </foreach>
    </select>

</mapper>
