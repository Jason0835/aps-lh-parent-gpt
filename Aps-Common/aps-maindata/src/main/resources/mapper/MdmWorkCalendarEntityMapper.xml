<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zlt.aps.maindata.mapper.MdmWorkCalendarEntityMapper">

    <insert id="copy" parameterType="com.zlt.aps.mp.api.domain.entity.MdmWorkCalendar">
        insert into T_MDM_WORK_CALENDAR(FACTORY_CODE, PROC_CODE, YEAR, MONTH, DAY, PRODUCTION_DATE,
                                        ONE_SHIFT_FLAG, TWO_SHIFT_FLAG, THREE_SHIFT_FLAG, DAY_FLAG, RATE
                                        CREATE_TIME, CREATE_BY, UPDATE_TIME, UPDATE_BY)
        select #{targetFactoryCode},
               #{targetProcCode},
               #{targetYear},
               #{targetMonth},
               `DAY`,
               STR_TO_DATE(CONCAT(#{targetYear}, '-', #{targetMonth}, '-', `DAY`), '%Y-%m-%d'),
               ONE_SHIFT_FLAG,
               TWO_SHIFT_FLAG,
               THREE_SHIFT_FLAG,
               DAY_FLAG,
               RATE,
               now(),
               #{createBy},
               now(),
               #{updateBy}
        from T_MDM_WORK_CALENDAR
        where IS_DELETE = 0
          AND FACTORY_CODE = #{sourceFactoryCode}
          and `YEAR` = #{sourceYear}
          and `MONTH` = #{sourceMonth}
          and PROC_CODE = #{sourceProcCode}
    </insert>

    <select id="selectMenuBtPermsByUserId" parameterType="Long" resultType="String">
        select distinct m.bt_perms
        from sys_menu m
        left join sys_role_menu rm on m.menu_id = rm.menu_id
        left join sys_user_role ur on rm.role_id = ur.role_id
        left join sys_role r on r.role_id = ur.role_id  and r.del_flag='0'
        where m.status = '0' and r.status = '0' and ur.user_id = #{userId}
    </select>
</mapper>
