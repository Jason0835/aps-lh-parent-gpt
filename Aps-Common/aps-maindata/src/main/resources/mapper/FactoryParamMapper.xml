<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zlt.aps.maindata.mapper.FactoryParamMapper">


    <select id="getFacParamList" parameterType="com.zlt.aps.mp.api.domain.entity.FactoryParam"
            resultType="com.zlt.aps.mp.api.domain.entity.FactoryParam">
        select * from T_MP_FACTORY_PARAM
        where is_delete = 0 and IS_SHOW = '1'
        <if test="factoryCode != null  and factoryCode != ''">and factory_code = #{factoryCode}</if>
        <if test="productTypeCode != null  and productTypeCode != ''">and product_type_code = #{productTypeCode}</if>
        <if test="paramCode != null and paramCode != ''">and PARAM_CODE like concat(concat('%', #{paramCode}), '%')</if>
        <if test="paramName != null and paramName != ''">and PARAM_NAME like concat(concat('%', #{paramName}), '%')</if>
        <if test="businessGroup != null and businessGroup != ''">and BUSINESS_GROUP like concat(#{businessGroup}, '%')</if>
    </select>

    <select id="getParamByBrandList" resultType="com.zlt.aps.maindata.domain.dto.ProductBrandDto">
        SELECT DICT_VALUE AS BRAND,
               DICT_LABEL AS BRAND_NAME
          FROM sys_dict_data
         WHERE DICT_TYPE = 'biz_brand_type'
           AND DICT_LABEL IN
        <foreach collection="brandNameList" item="brandName" index="index" open="(" separator="," close=")">
            #{brandName,jdbcType=VARCHAR}
        </foreach>
    </select>
</mapper>
