<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zlt.mix.common.engine.mapper.SyncDataLogsMapper">
	<!-- 查询同步日志 -->
    <select id="getSyncDataLogs" resultType="com.zlt.mix.common.engine.domain.SyncDataLogs">
	    SELECT T.DATA_VERSION,
		       T.PARAMS,
		       CASE
		         WHEN T.STATUS = 3 THEN
		          2	/*处理失败*/
		         WHEN COUNT(1) > 1 THEN
		          1 /*处理成功*/
		         ELSE
		          3 /*处理中*/
		       END STATUS,
		       T.MSG
		  FROM AUX_REQ_SYNC_DATA_LOGS T
		  LEFT JOIN AUX_REQ_SYNC_DATA_LOGS_HIS H
		    ON T.DATA_VERSION = H.DATA_VERSION
		 WHERE T.DATA_VERSION = #{dataVersion}
		 GROUP BY T.DATA_VERSION, T.STATUS, T.PARAMS, T.MSG
    </select>

	<!-- 查询请求日志 -->
    <select id="getReqDataLogs" resultType="com.zlt.mix.common.engine.domain.SyncDataLogs">
	    SELECT T.DATA_VERSION,
		       T.PARAMS,
		       CASE
		         WHEN T.STATUS = 3 THEN
		          2	/*处理失败*/
		         WHEN T.STATUS in (1, 2, 6) THEN
		          1 /*处理成功*/
		         ELSE
		          3 /*处理中*/
		       END STATUS,
		       T.MSG
		  FROM AUX_REQ_SYNC_DATA_LOGS T
		 WHERE T.DATA_VERSION = #{dataVersion}
		 GROUP BY T.DATA_VERSION, T.STATUS, T.PARAMS, T.MSG
    </select>
</mapper>
