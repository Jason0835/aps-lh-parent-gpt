# 2026-08-24

## 提前生产逻辑梳理（代码级）
- 每日阶段顺序（NewSpecProductionStrategy.scheduleCurrentBusinessDay L564-641）：CARRY_OVER → 换活字块反选检测(正常) → NORMAL_RESOURCE_COMPETITION → 反选收口 → rebuildScheduledMachineCountMap 冻结资源 → EARLY_PRODUCTION（3a 提前在机延续 scheduleCarryOverSkus(true) → 3b 反选检测 → 3c 候选选机 → 3d 收口）→ 日志分节 → FINALIZE（L3017-3041 日终收口）。
- LEGACY_SHORTAGE_OR_ENDING 已下线：无 setCurrentPhase 入口，candidate 逻辑保留但统一返回 false（L2076-2079）。
- 提前生产机台范围：仅限当前业务日 dayShifts 内可真实开产的机台。证据：filterByEarliestAvailableShift L7328-7329（目标班次只从 dayContext.getDayShifts() 解析）、L3905-3907、L3932（当日全部班次无可开产候选→延期）、L4487-4494（换模须在日窗口内）。T+1 才空闲的机台当日选不中。
- 提前生产跨日：T 日提前上机绑定在 T+1 不进正常阶段（T+1 dayN=0 非正常候选），而是后置到 T+1 提前生产阶段续排（shouldRunBindingInEarlyProductionPhase L2382-2395：isEarlyProduction=YES 且当日原始 dayN<=0）；原机台被正常任务换产则重新选机；到真正计划日（如 T+2）转正常在机任务走阶段1。续排受结构机台数硬控（L2307-2328）。
- 结构计划硫化机台数取数：来源月计划统计表 MpMonthPlanStatistics.dayN.lhMachines，按「业务日+结构」加载（LhBaseDataServiceImpl.loadMonthPlanStatistics L1004），无平移。每日有效机台数 = resolveEffectiveStructurePlanMachineCount（EarlyProductionChecker L616-636）：当日>0 用当日，当日=0 回退取 firstFuturePlanDate（最早未来计划日，非顺移）的值。T 与 T+1 都取 T+2 的机台数。decision.structurePlanMachineCounts（窗口 T~T+2 原始值）仅用于结果备注 buildRemark，不参与门禁。
- 提前生产机台数两取数点（均结构维度，无 SKU 维度计划机台数；SKU 经 structureName 关联）：①准入场景判定用原始值 getStructurePlanMachineCount(当日/最早未来计划日)（checkEarlyProduction L106-109），current>0→NORMAL、current=0&future>0→SWITCH；②增机门禁用有效值 resolveEffectiveStructurePlanMachineCount，入口 checkEarlyProduction L110（已排>计划→拒 L150）、选机硬控 canUseMachineForEarlyProduction L455-481（<计划可新增、=计划仅复用已计入结构机台 hasStructureScheduledMachine L476-478、>计划拒），硬控下沉在候选确定后资源扣减前（NewSpecProductionStrategy L4180-4182）+ 跨日续排 L2313-2315。已排数 = 结构物理机台去重计数（getStructureScheduledMachineCount L1452-1471）。SKU 级已排机台数仅日志诊断。数据加载范围特意扩到[窗口T日, 窗口末日+N]（L919-920 注释）保证 futurePlanDate 可读。
