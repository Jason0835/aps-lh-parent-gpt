<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zlt.aps.cd90.engine.mapper.Cd90EngineBigRollMapper">
	<!-- 查询帘线大卷配置信息 -->
    <select id="listCd90BigRoll" resultType="com.zlt.aps.cd90.engine.vo.Cd90BigRollVo">
        SELECT BIG_ROLL_CODE,
               IFNULL(MAX(ACT_CLOTH_LENGTH), 0) CLOTH_LENGTH
          FROM T_CD90_BIG_ROLL
         WHERE DEL_FLAG = '0'
         GROUP BY BIG_ROLL_CODE
    </select>

    <select id="listCd90CurlLength" resultType="com.zlt.aps.cd90.api.domain.entity.Cd90CurlLength">
        SELECT CLOTH_CODE,
               IFNULL(MAX(CURL_LENGTH), 0) CURL_LENGTH
          FROM T_CD90_CURL_LENGTH
         WHERE IS_DELETE = 0
         GROUP BY CLOTH_CODE
    </select>
</mapper>
